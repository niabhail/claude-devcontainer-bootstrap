# firewall-allowlist.txt
# Domain/IP Egress Allowlist -- critical for secure devcontainer network policy
# One domain or IP (or CIDR) per line. Lines starting with # are comments.
# Edit only with approval from your project/network lead.
#
# Example:
# api.mycompany.com
# 203.0.113.0/24
# *.internal.corp